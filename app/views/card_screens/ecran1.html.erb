<div class="container-rooms-join-titre">
  <h1>Quelles sont tes preferences ?</h1>
  </div>
  <br>
<div class="container">
<div class="container_cards">
  <% @choice_categories.each do |choice_category| %>
  <% if choice_category.rank == 0 %>
    <div class="col-4">
      <div class="card">
        <img class="card_img"
          src="https://i.pinimg.com/originals/c1/c5/3c/c1c53cda4fa51316d942ca91cf0d40ac.jpg">
        <div class="card_title"><%= choice_category.category.name %></div>
          <div class="card_description">
            <p>Lorem Ipsum copy in various charsets and languages for layouts. ... by this text-generator is
              suitable for greeking, typesetting, layouts for websites, and ... Using dummy content or fake
            </p>
          </div>
        <div class="card_vote"><%= link_to image_tag("heart1.png"), room_choice_category_path(id: choice_category.id, :rank => 3), :method => :patch%>
        <%= link_to image_tag("heart2.png"), room_choice_category_path(id: choice_category.id, :rank => 1), :method => :patch%>
        <%= link_to image_tag("skull.png"), room_choice_category_path(id: choice_category.id, :rank => -100), :method => :patch %></div>
      </div>
    </div>
  <% end %>
  <% end %>
</div>
</div>

<br>
<br>
<br>
<br>
<br>
<div class="container">
<% unless @choice_categories[0].rank == 0 %>
<h1 style="font-size:80px">TO BE CONTINUED</h1>
<%= @choice_categories[0].rank + 1 %>
<% end %>
</div>
