<div class="d-flex flex-wrap" style="max-width:100vw">
  <% @categories.each do |category| %>
    <div class="col-4">
      <div style="height:200px;border-radius:5px;background-color:wheat" class="p-4 mb-4"><%= category.name %>
      <div class="d-flex"><%= link_to "1" %><%= link_to "2" %><%= link_to "3" %></div>
      </div>
    </div>
  <% end %>
</div>

  <%= link_to "+1", room_choice_category_path(:id => ChoiceCategory.find(1), :rank => +1), :method => :patch, class: "btn-reservation" %>  </div>
